CP Notifications implementation progress
========================================

2026-08-12 15:58:08 NZST
Todo: Scaffold the Statamic 6 / Laravel 13 addon package as `ghijk/cp-notifications`.
Done: Completed publishable Composer metadata, declared supported PHP and Statamic versions, replaced the placeholder README copy, and added package/provider scaffold tests.
Tests: composer validate --strict; vendor/bin/phpunit --testdox
Notes: Statamic 6 supports Laravel 12 and 13, so the addon keeps Statamic's compatible range while documenting Laravel 13 as the recommended target.

2026-08-12 16:02:00 NZST
Todo: Register the addon service provider, publishable config, routes, migrations, commands, and CP assets.
Done: Registered the addon config and CP route sources, migration loading/publishing, an install command, and the Statamic Vite entrypoints; added the standard frontend build manifest and configuration.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Feature routes, migrations, and Vue components will be added to these registered extension points with their corresponding todos.

2026-08-12 16:06:00 NZST
Todo: Add `config/cp-notifications.php` with acknowledgement driver, enforcement, retention, and nudge settings.
Done: Added environment-aware acknowledgement driver and file path, strict/modal enforcement, Inbox retention, and nudge sender configuration with the PRD defaults.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Null Inbox retention means indefinite history; a null nudge sender delegates to the host application's mail configuration.

2026-08-12 16:10:00 NZST
Todo: Register the Inbox CP navigation item and links to management/reporting surfaces.
Done: Added a Notifications navigation section with Inbox, Manage, and Reports links, registered their CP routes, and provided initial surface views for progressive enhancement by later todos.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Each navigation item declares its final permission ability; permission definitions and endpoint authorization are completed by their dedicated todos.

2026-08-12 16:14:00 NZST
Todo: Register `view notifications` (available to all users by default).
Done: Registered the Inbox permission in a dedicated CP Notifications permission group and made the Inbox navigation available by default to authenticated CP users instead of requiring role assignment.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The permission remains registered for discoverability and future policy integration, while default Inbox availability is expressed by the absence of an additional navigation authorization gate.

2026-08-12 16:18:00 NZST
Todo: Register `manage notifications`.
Done: Registered the management ability in the CP Notifications permission group; the Manage navigation item already requires this exact ability.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Collection policies and management endpoints will consume this ability as those features are implemented.

2026-08-12 16:20:00 NZST
Todo: Register `view notification reports`.
Done: Registered the reporting ability in the CP Notifications permission group; the Reports navigation item requires the same ability.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Report views, CSV export, and manual reminder authorization will share this ability.

2026-08-12 16:22:00 NZST
Todo: Register `bypass notifications` (granted to the creator by default).
Done: Registered the global enforcement-bypass ability in the CP Notifications permission group.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Per the agreed policy, explicit holders bypass all gating; notice authors bypass only their own notice without receiving a persistent global permission grant. Author resolution is implemented with active-stack gating.

2026-08-12 16:24:00 NZST
Todo: Register `purge notifications`.
Done: Registered the expired-notification purge ability and completed the five-permission CP Notifications group.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The eventual clear-out action will require this ability in addition to explicit confirmation and eligibility checks.

2026-08-12 16:28:00 NZST
Todo: Create a routeless, CP-only `notifications` collection.
Done: Expanded the install command with an idempotent collection installer that creates the notifications collection without routes or required slugs and refuses to overwrite a conflicting routed collection.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: With no collection route, notification entries have no public content URL and are managed exclusively through control-panel surfaces.

2026-08-12 16:34:00 NZST
Todo: Create the notification blueprint with content, severity, behavior, audience, scheduling, and nudge fields.
Done: Added an installer-owned Notification blueprint with required title/Bard content, independent severity and blocking controls, optional priority, structured audience selectors, time-aware start/end scheduling, and structured nudge settings.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Audience and nudge values are stored as grouped objects matching the PRD data shape; their behavioral validation is implemented in subsequent todos.

2026-08-12 16:38:00 NZST
Todo: Add validation requiring at least one effective audience target before publishing.
Done: Added server-side EntrySaving validation that permits incomplete drafts but requires all-users targeting or at least one non-empty role, group, or explicit-user selector for published notifications.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The rule validates configured targets at publication; live membership expansion and de-duplication are handled by the audience resolver todos.

2026-08-12 16:42:00 NZST
Todo: Force or treat `snoozeable` as false whenever `blocking` is true.
Done: Added an EntrySaving normalizer ahead of validation that forcibly stores blocking notifications as non-snoozeable while preserving advisory snooze settings.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Server-side normalization protects every save path, including custom clients that bypass control-panel field visibility.

2026-08-12 16:46:00 NZST
Todo: Ensure notices are global across multisite installations.
Done: Made the notifications collection explicitly canonical to the default site with localization propagation disabled, so a single entry set drives every control-panel site.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Notification resolvers will query this default-site collection regardless of the currently selected control-panel site.

2026-08-12 16:50:00 NZST
Todo: Preserve Statamic's native author, revisions, and draft/published lifecycle.
Done: Enabled native collection revisions, configured new notifications as drafts by default, and retained Statamic's native entry author and publication properties without a parallel custom lifecycle.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Statamic's entry controller assigns the authenticated creator to the native author value; publishing continues through Statamic's normal working-copy and publish APIs.

2026-08-12 16:55:00 NZST
Todo: Define the immutable `Acknowledgement` data object.
Done: Added a final readonly acknowledgement value object with stable record, notification, user, and immutable timestamp fields plus array/JSON hydration and serialization.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Storage drivers will use snake_case serialized keys while application code receives typed camelCase properties and CarbonImmutable timestamps.

2026-08-12 16:58:00 NZST
Todo: Define the transient, single-use `Snooze` data object.
Done: Added a final readonly snooze value object with notification/user identity, immutable expiry, array/JSON serialization, and shared active-at-time behavior.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: A snooze is active strictly before snoozed_until and inactive at the exact expiry boundary.

2026-08-12 17:02:00 NZST
Todo: Create `AcknowledgementRepository` with find, idempotent record, forNotification, and forUser methods.
Done: Defined the typed acknowledgement persistence boundary with lookup, once-only recording, and notification/user collection queries.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: record accepts an optional CarbonImmutable timestamp for deterministic driver contract tests; implementations must return the original record on duplicate calls.

2026-08-12 17:06:00 NZST
Todo: Create the equivalent `SnoozeRepository` contract.
Done: Defined the typed snooze persistence boundary with lookup, idempotent once-only recording, and notification/user collection queries.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: record accepts an optional CarbonImmutable expiry for deterministic driver and 24-hour boundary tests; implementations must return the original snooze when a user attempts to spend it again.

2026-08-12 17:14:00 NZST
Todo: Implement the Eloquent acknowledgement and snooze repositories.
Done: Added database-backed acknowledgement and snooze repositories with typed hydration, idempotent insert-and-read recording, deterministic collection queries, UUID acknowledgement IDs, and a default 24-hour snooze duration.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The repositories rely on a unique notification/user key to make insertOrIgnore concurrency-safe; the package migrations and production indexes are added by the following dedicated todos.

2026-08-12 17:20:00 NZST
Todo: Add migrations and indexes for acknowledgement and snooze tables.
Done: Added a reversible production migration for acknowledgement and snooze storage, including primary acknowledgement IDs and indexes for notification, user, acknowledgement time, and snooze expiry lookups.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Composite notification/user uniqueness is intentionally added and verified by its immediately following dedicated todo.

2026-08-12 17:25:00 NZST
Todo: Enforce uniqueness per `(notification, user)` at the database level.
Done: Added composite unique constraints for notification/user pairs to both acknowledgement and snooze tables, making once-only records enforceable independently of application code.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Migration tests verify both unique-index definitions and real duplicate-insert failures for each record type.

2026-08-12 17:34:00 NZST
Todo: Implement file repositories using one YAML file per notice/user record under the configured storage path.
Done: Added YAML-backed acknowledgement and snooze repositories with one safely addressed file per notification/user pair, typed hydration, idempotent sequential recording, and notification/user collection queries.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: URL-safe base64 path segments prevent record IDs containing separators from escaping their storage tree; concurrent create-if-absent hardening is handled by the next todo.

2026-08-12 17:43:00 NZST
Todo: Make file writes atomic and safe under concurrent requests.
Done: Added a shared atomic create-if-absent writer that fully writes a same-directory temporary file before publishing it with an exclusive hard link; losing requests read and return the winning immutable record.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: A six-process contention test verifies that concurrent acknowledgement attempts leave exactly one complete, parseable YAML record with no temporary-file residue.

2026-08-12 17:49:00 NZST
Todo: Implement `auto`, `eloquent`, and `file` driver resolution; auto-detect `statamic/eloquent-driver`.
Done: Added a validated repository driver resolver that preserves explicit file/eloquent configuration and resolves auto through Composer's installed-package registry.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Detection can be overridden explicitly in focused tests; unsupported driver names fail early with a configuration-specific exception.

2026-08-12 17:56:00 NZST
Todo: Bind the selected repositories into Laravel's container.
Done: Registered lazy singleton bindings for both repository contracts, resolving the shared configured driver and constructing database- or file-backed implementations with their required Laravel dependencies.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Lazy resolution ensures Statamic's addon config has booted before the driver and configured file storage path are read.

2026-08-12 18:02:00 NZST
Todo: Ensure acknowledgements cannot be updated, revoked, or deleted through application APIs.
Done: Hardened the append-only acknowledgement boundary by making both concrete drivers final and verifying their only public persistence operations are find, record, forNotification, and forUser.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Application services receive the mutation-free contract from the container, while the acknowledgement value itself remains final and readonly.

2026-08-12 18:08:00 NZST
Todo: Ensure a snooze lasts 24 hours and cannot be used a second time for the same notice/user.
Done: Finalized both snooze drivers around create-once behavior and verified database and file defaults expire exactly 24 hours after creation while repeat attempts retain the original expiry and single record.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Snooze activity remains strict-before expiry, so the snooze is inactive at the exact 24-hour boundary while its retained record prevents reuse.

2026-08-12 16:48:19 NZST
Todo: Resolve audience membership from `all`, roles, groups, and explicit user IDs.
Done: Added an audience matcher that evaluates notification entries or audience arrays against Statamic users using all-user, effective-role, group, and explicit-user rules.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Role matching uses Statamic's effective `hasRole` behavior, so roles inherited through groups are included in membership resolution.

2026-08-12 16:49:07 NZST
Todo: De-duplicate users targeted through multiple audience rules.
Done: Added an audience resolver that scans the current Statamic user repository once, filters through the shared matcher, and returns one user per stable user ID even when rules or repository results overlap.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The resolver preserves repository order while returning Statamic's native UserCollection type for downstream inbox and reporting use.

2026-08-12 16:49:42 NZST
Todo: Resolve the targeted user set at query/view time.
Done: Kept audience expansion deliberately uncached and verified that every resolution reads the current user repository and current role/group membership.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: A user moving into or out of a targeted role is reflected on the next inbox/report query without modifying or republishing the notice.

2026-08-12 16:50:30 NZST
Todo: Implement active-window checks using published status, `start_date`, optional `end_date`, and the site timezone.
Done: Added a published-notice active-window evaluator using the application/site timezone, inclusive start boundaries, exclusive end boundaries, and indefinite open-ended windows.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Drafts, future notices, expired notices, and malformed notices without the required start date are inactive; timezone-aware instants are normalized before comparison.

2026-08-12 16:51:43 NZST
Todo: Filter acknowledged notices from the active stack.
Done: Added the active-stack resolver, filtering notices by live audience, active window, and the absence of a persisted acknowledgement for the current notification/user pair.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Audience and scheduling filters run before repository lookups, avoiding acknowledgement reads for notices that cannot enter the user's active stack.

2026-08-12 16:52:33 NZST
Todo: Filter notices with a currently active snooze from the active stack.
Done: Extended active-stack resolution to exclude notices whose retained one-time snooze is active for the current user.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: At the exact `snoozed_until` instant the notice returns to the stack; expired snooze records remain stored to prevent reuse.

2026-08-12 16:53:13 NZST
Todo: Keep bypass users in audience/inbox results while excluding them only from gating.
Done: Added a dedicated gating-stack projection that empties enforcement results for users with the bypass permission without mutating or filtering their visible active notices.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Audience, Inbox, and reporting services remain permission-agnostic; bypass is applied only at the enforcement boundary.

2026-08-12 16:54:17 NZST
Todo: Order notices by explicit priority override, then severity (`critical`, `warning`, `info`), then oldest `start_date`.
Done: Added deterministic active-stack ordering with explicit lower-number priorities first, followed by critical, warning, and info severity, then oldest start time. Also hardened an existing parent-directory creation race exposed by repeated concurrency tests.
Tests: vendor/bin/phpunit (three consecutive runs); composer validate --strict; git diff --check
Notes: Any explicit numeric priority sorts ahead of the unprioritized severity queue. Exact null and full-tie precedence is verified by the following dedicated todo.

2026-08-12 16:54:52 NZST
Todo: Define and test the exact precedence/direction for nullable and tied priorities.
Done: Defined and verified the complete sort key: explicit priorities before null/blank, lower numbers first, then critical/warning/info, oldest start time, and notification ID as the deterministic final tie-breaker.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Equal-priority notices still use severity and start time; unprioritized notices never outrank an explicitly prioritized notice regardless of severity.

2026-08-12 16:55:32 NZST
Todo: Compute `locked` as true once the first acknowledgement exists.
Done: Added a notification lock service that derives lock state directly from the immutable acknowledgement repository and becomes true with the first record.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Lock state is derived rather than copied into entry content, preventing stale flags and keeping both persistence drivers authoritative.

2026-08-12 16:56:18 NZST
Todo: Prevent edits to locked notice content and settings on the server.
Done: Registered an early EntrySaving listener that rejects every save path for locked notification entries while leaving unlocked notifications and unrelated collections untouched.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Enforcement runs before behavior normalization and audience validation, so rejected requests cannot mutate locked notice data in memory before failure.

2026-08-12 16:57:36 NZST
Todo: Render locked notices as read-only in the publish form.
Done: Added an EntryBlueprintFound listener that projects every field in a locked notification blueprint with Statamic's native read-only visibility while leaving unlocked forms editable.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The stored blueprint remains unchanged; read-only visibility is applied only to the locked entry's runtime publish-form blueprint.

2026-08-12 16:58:03 NZST
Todo: Provide a clear validation/error message directing admins to create a superseding notice.
Done: Expanded locked-save validation to explain that acknowledgements caused the lock and instruct administrators to create a superseding notification for corrections.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The message is returned as a normal validation error, so CP and custom save clients receive the same actionable response.

2026-08-12 17:00:07 NZST
Todo: Add an authenticated endpoint returning the current user's ordered active stack.
Done: Added a CP-authenticated JSON endpoint that queries the canonical notification collection, resolves the current user's live ordered stack, and returns the overlay-facing notice fields.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The route inherits Statamic's authenticated CP middleware and always queries the default-site notification set, preserving global multisite behavior.

2026-08-12 17:01:06 NZST
Todo: Add idempotent acknowledgement/confirm endpoint(s).
Done: Added an authenticated acknowledgement endpoint that validates live activity and audience eligibility, records through the once-only repository, and returns the original record on repeat requests.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Already-acknowledged retries remain successful even after the notice leaves its active window, supporting safe client retries without creating duplicate records.

2026-08-12 17:01:40 NZST
Todo: Require the explicit “I have read and understand” confirmation value server-side.
Done: Required the literal JSON boolean `confirmed: true` before any acknowledgement lookup or write and added an actionable validation response for missing, false, or string values.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Strict boolean comparison prevents generic truthy payloads from being treated as the user's explicit attestation.

2026-08-12 17:02:52 NZST
Todo: Add the single-use 24-hour snooze endpoint for eligible advisory notices.
Done: Added an authenticated snooze endpoint restricted to active, targeted, non-blocking, snoozeable advisories, returning the repository's exact 24-hour expiry.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Any existing snooze record causes a conflict even after expiry, enforcing the one-time allowance independently of current snooze activity.

2026-08-12 17:04:46 NZST
Todo: Build and register a global Vue 3 overlay using Statamic UI components.
Done: Added a globally appended Vue 3 overlay using Statamic UI card and badge components, accessible dialog semantics, severity styling, and authenticated stack loading; added the Tailwind 4 build dependency and compiled distributable assets.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; npm run build; git diff --check
Notes: The overlay is appended through Statamic's global component manager, so it mounts across CP views rather than requiring each page template to include it.

2026-08-12 17:05:32 NZST
Todo: Render the top notice and enforce top-down clearing.
Done: Made the ordered stack's index-zero notice the overlay's sole renderable current item and verified that no iteration or navigation exposes later notices before it clears.
Tests: vendor/bin/phpunit --testdox; npm run build; composer validate --strict; git diff --check
Notes: Later notices remain in component state for the progress count but have no render path or action surface until the server refresh promotes the next item.

2026-08-12 17:06:32 NZST
Todo: Make blocking notices confirm-only and impossible to dismiss or snooze.
Done: Added an explicit attestation checkbox and Statamic UI confirmation button to the overlay, wired to the strict acknowledgement endpoint with CSRF protection and no dismiss or snooze control for blocking notices.
Tests: vendor/bin/phpunit --testdox; npm run build; composer validate --strict; git diff --check
Notes: Confirmation stays disabled until the user actively checks the attestation; the server independently requires the literal boolean confirmation value.

2026-08-12 17:07:08 NZST
Todo: Allow eligible advisory notices to be confirmed or snoozed.
Done: Added a 24-hour Snooze action for notices that are both advisory and snoozeable while retaining the explicit confirmation path for every advisory.
Tests: vendor/bin/phpunit --testdox; npm run build; composer validate --strict; git diff --check
Notes: The UI condition mirrors server eligibility: blocking notices never render Snooze even if malformed stored data claims they are snoozeable.

2026-08-12 17:07:47 NZST
Todo: Refresh/advance the stack after each successful action.
Done: Made successful confirmation and snooze actions reset attestation and re-fetch the authoritative ordered stack, allowing the server-selected next notice to advance into view.
Tests: vendor/bin/phpunit --testdox; npm run build; composer validate --strict; git diff --check
Notes: The client does not optimistically shift its local array, so concurrent audience, scheduling, acknowledgement, and priority changes are reflected during every advance.

2026-08-12 17:08:29 NZST
Todo: Handle expired, already-acknowledged, and concurrently updated notices gracefully.
Done: Added centralized action reconciliation that refreshes on successful idempotent responses and stale 404/409 states, while surfacing retryable validation and network errors in an accessible alert.
Tests: vendor/bin/phpunit --testdox; npm run build; composer validate --strict; git diff --check
Notes: Expired, deleted, concurrently snoozed, and otherwise stale notices disappear through authoritative refresh; transport failures leave the current notice and controls available for retry.

2026-08-12 17:09:24 NZST
Todo: Build the acknowledgement interstitial for unresolved blocking notices.
Done: Added a dedicated authenticated CP interstitial route and view plus a reusable resolver that derives ordered blocking notices from the live active and permission-aware gating stacks.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The interstitial renders only when an unresolved blocking notice exists and otherwise returns users to the CP; the global confirmation overlay remains the action surface.

2026-08-12 17:10:27 NZST
Todo: Add CP middleware that guards routes when enforcement is `strict`.
Done: Added and registered authenticated-CP middleware that evaluates the live blocking stack only in strict mode and redirects gated requests to the acknowledgement interstitial.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The middleware lives after Statamic authentication and exempts the addon's interstitial/API action routes so users can fetch and clear the notice that caused gating.

2026-08-12 17:11:32 NZST
Todo: Redirect users with an active blocking notice to the interstitial.
Done: Added a behavioral middleware test using a real published blocking entry, live audience/window filtering, empty acknowledgement/snooze state, and a non-bypass user to prove CP requests redirect to the interstitial.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The test exercises the composed production resolver rather than stubbing the redirect decision, covering canonical collection querying and gating-stack behavior together.

2026-08-12 17:12:20 NZST
Todo: Avoid redirect loops and permit required assets, acknowledgement routes, and logout.
Done: Centralized strict-gate exemptions for the interstitial, notification APIs, logout/session maintenance, and CP thumbnail/SVG/PDF delivery routes.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: The interstitial and action wildcard prevent self-redirect loops; static compiled addon assets are served outside the authenticated CP route group and therefore never enter this middleware.

2026-08-12 17:13:01 NZST
Todo: Short-circuit route gating for users with `bypass notifications`.
Done: Moved the bypass-permission check ahead of blocking-stack resolution so permission holders immediately continue through the CP middleware.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: This avoids collection and persistence-driver reads for bypass users while leaving their separate visible active stack unchanged for overlay and Inbox access.

2026-08-12 17:13:39 NZST
Todo: In `modal` mode, disable route guarding while retaining the overlay.
Done: Added a behavioral test proving modal enforcement returns before user/stack resolution and confirmed the global overlay remains appended independently of middleware mode.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Strict and modal share the same overlay and action APIs; only strict mode activates route redirection.

2026-08-12 17:14:35 NZST
Todo: Build the user's Inbox showing only notices targeted at that user.
Done: Replaced the placeholder Inbox with a user-specific controller, canonical live resolver, and CP listing view restricted to published notices matching the current user's audience.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Audience membership is evaluated on every Inbox request, so role/group changes immediately affect visibility and draft notices never leak to recipients.

2026-08-12 17:15:46 NZST
Todo: Show active notices and retained history, including previously read notices.
Done: Enriched Inbox rows with acknowledgement and snooze state, separated actionable active notices from history, and displayed recorded read timestamps while retaining acknowledged entries.
Tests: vendor/bin/phpunit --testdox; composer validate --strict; git diff --check
Notes: Active rows sort first; published targeted notices remain in history after acknowledgement, expiry, or while an active snooze temporarily removes them from the action stack.

2026-08-12 17:17:08 NZST
Todo: Apply retention.inbox_days; treat null as indefinite retention.
Done: Filtered expired Inbox history against the configured retention window with an inclusive cutoff, while preserving notices without an end date and treating null retention as unlimited.
Tests: vendor/bin/phpunit; composer validate --no-check-publish; git diff --check
Notes: Retention is evaluated in the application timezone and affects Inbox visibility only; the active window and acknowledgement records remain unchanged.

2026-08-12 17:17:51 NZST
Todo: Ensure expired, unacknowledged advisories stop appearing as active but remain reportable.
Done: Verified with an end-to-end Inbox resolver test that an unacknowledged advisory immediately becomes inactive at expiry while remaining in retained history.
Tests: vendor/bin/phpunit; composer validate --no-check-publish; git diff --check
Notes: Active state derives from the exclusive end boundary, while reportable history is governed separately by Inbox retention and does not require an acknowledgement.

2026-08-12 17:19:05 NZST
Todo: Configure the collection listing and publish form for notice management.
Done: Configured notifications to sort by descending start date and exposed concise title, severity, blocking, start, and end columns while retaining the complete tabbed publish blueprint.
Tests: vendor/bin/phpunit; composer validate --no-check-publish; git diff --check
Notes: Idempotent installs also repair the collection sort settings; body and nested group fields remain edit-form-only, while optional snooze and priority fields stay available through column customization.

2026-08-12 17:20:18 NZST
Todo: Display appropriate status indicators for draft, scheduled, active, expired, and locked notices.
Done: Added a computed management status column and lifecycle classifier covering all five states with application-timezone scheduling and acknowledgement-backed locking.
Tests: vendor/bin/phpunit; composer validate --no-check-publish; git diff --check
Notes: Locked takes precedence because acknowledged notices are immutable; expiration uses the same exclusive end boundary as active-stack resolution.

2026-08-12 17:22:01 NZST
Todo: Build a per-notice report authorized by view notification reports.
Done: Replaced the report placeholder with a canonical notification index and per-notice report route, both enforcing the reporting permission server-side.
Tests: vendor/bin/phpunit; composer validate --no-check-publish; git diff --check
Notes: Per-notice lookup rejects entries outside the notifications collection or canonical site; the computed management callback was also made capture-free so entries remain queue-serializable.
